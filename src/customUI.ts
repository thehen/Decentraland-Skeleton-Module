import * as ui from '@dcl/ui-scene-utils';

export class CustomUI {
    public ShowPrompt = () => {
        var purchasePrompt = new ui.CustomPrompt(ui.PromptStyles.LIGHT, undefined, undefined, false)
    }
}